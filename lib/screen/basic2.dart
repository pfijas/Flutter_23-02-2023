import 'package:flutter/material.dart';

class Basic2 extends StatelessWidget {
  const Basic2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("fluoiiiii"),
      ),
    );
  }
}
